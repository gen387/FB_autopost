Install:
- python
- pip

Install using pip
- selenium
- libraries as in the python file (by, random, time etc. if not available with Selenium)

Download chromedriver and place it inside the catalog with fb_bot.py file.

